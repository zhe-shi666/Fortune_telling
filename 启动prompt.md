这次项目开发从 Pencil MCP 开始。

请严格按照当前项目中的这些文件协同工作：
- AGENTS.md
- docs/rules.md
- docs/app.md
- docs/domain.md
- docs/api.md
- skills/页面生成助手/SKILL.md
- skills/逻辑实现助手/SKILL.md
- skills/测试助手/SKILL.md

我的 App 想法是：
【在这里写一句话产品描述】

启动要求：
1. 先读取 AGENTS.md 和 docs/rules.md。
2. 通过 Pencil MCP 获取当前 UI 设计树，把它作为页面结构和 MVP 页面来源。
3. 如果 Pencil MCP 没有可用设计树、Pencil 文件为空、或当前没有打开可用设计，则退回到仅凭一句话产品描述推进，并在假设中明确写出“UI 结构来自推断，非设计稿”。
4. 如果 docs/app.md 还是模板或信息不足，基于“一句话产品描述 + Pencil MCP UI 结构”自动补全 docs/app.md 的 v0。
5. 如果 docs/domain.md 还是模板或信息不足，基于 docs/app.md 和 Pencil MCP 中可推断的页面语义，自动补全 docs/domain.md 的 v0。
6. 如果 docs/api.md 还是模板或信息不足，基于 Pencil MCP 设计树、docs/app.md 页面清单，以及可推断的 MVP 接口需求，自动补全 docs/api.md 的 v0。
7. 明确写出关键假设，不要把猜测伪装成已确认事实。
8. 第一个页面优先选择：关键用户流程第一步出现、且在 Pencil MCP 中已有明确结构的页面。
9. 用页面生成助手先生成这个页面的骨架、契约、mock 和 ui-layout-report。
10. 如果项目尚无运行入口，则在第一个页面骨架完成后，立即装配最小可运行的 app 壳，包括：运行入口、根路由、首个 MVP 页面装配、最小 mock 数据，让我可以打开进行人工审核。
11. 严格按“页面生成助手 -> 逻辑实现助手 -> 测试助手”的顺序推进当前 feature。
12. 页面阶段优先使用 Pencil MCP 作为 UI 起点；对结构不清晰的区域，按页面生成助手的 confidence 规则降级处理，不要强行推断复杂交互。
13. 将 Pencil 设计中的设备外框、系统状态栏、Home Indicator 视为系统 UI 或设计容器，不转成业务页面代码。
14. 状态栏时间、电量、信号、Wi-Fi、刘海、灵动岛、底部手势条只作为安全区和布局参考，不生成业务组件。
15. 只有当设计明确要求自绘状态栏或设备展示效果时，才允许保留这类元素，并标记为装饰性 UI。
16. 如果业务规则不足，优先在 docs/domain.md 中补保守假设；如果接口契约不足，优先在 docs/api.md 中补最小接口假设，再继续逻辑和测试阶段。
17. 每轮只推进一个 feature，不要同时铺开多个页面。
18. 当 docs/app.md 页面清单中的所有 MVP 页面都完成页面、逻辑、测试三个阶段后，停止循环，输出整体完成摘要，并等待我确认下一步。

输出要求：
- 先执行，再汇报
- 在第一个页面骨架完成后，优先装配可运行壳供人工审核
- 之后按正常 skill 顺序推进，不以“快速可运行”为由跳过报告、交接条件或测试阶段
- 每轮汇报说明：当前阶段、当前页面、关键假设、阻塞项、下一步
- 如果遇到 AGENTS.md 第 6 节定义的停止条件，必须立即停止并请求我确认
